# FY-005 SaaS empty-state onboarding

软件空状态引导

**Origin:** Flyne AI · MIT · **Status:** conceptual, not tested

**Category:** software · **Task:** ref2va · **Target:** 8 seconds, 16:9

**Reference map:** Image 1 = approved blank dashboard; Image 2 = approved populated state; synthetic data only.

**Use conditions / 使用条件:** Confirm a compatible route / 需先确认平台支持。Prepare the listed references and verify duration and input roles; do not treat multiple references as a first/last-frame pair. This is not ready to paste unchanged into the free tool. [Guide / 说明](../../docs/recipe-usage.md).

## Copy-ready prompt

```text
Create a restrained onboarding concept using the two supplied interface states. For two seconds show the empty dashboard from Image 1. Over the next four seconds, one cursor selects the existing create button; a single placeholder card appears in its designated panel. End for two seconds in the layout of Image 2. Preserve panel boundaries, type hierarchy and navigation location. No invented controls, user names, revenue numbers or testimonials. No dialogue. Use the shot as motion previsualization; exact text and cursor alignment will be rebuilt from the real UI.
```

**Acceptance:** Review layout and state logic; never present a speculative control as a shipped feature.

**Recovery:** If text warps, rebuild the entire screen in a motion editor.

**Production value:** Tests onboarding motion before implementation; not a substitute for a screen recording.

The duration is a creative target, not an API parameter guarantee. Select a compatible route in the [workflow guide](../../docs/workflows.md), record results with the [run template](../../templates/run-record.json), and verify input rights before use.
