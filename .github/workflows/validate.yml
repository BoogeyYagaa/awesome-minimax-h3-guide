name: Validate guide
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Validate checked-in catalog, links and provenance
        run: python3 scripts/validate.py
      - name: Test maintenance behavior
        run: python3 -m unittest discover -s scripts -p 'test_*.py'
